import pymongo
import json
import os


'''
## 신문 raw data after predicted
for i in range(1,15):
    os.system(f'mongoimport --db raw_data --collection newses --file predict_{i}.json')

## 꺾은선
os.system(f'mongoimport --db final_data --collection line_graph --file line_graph.json')
'''


